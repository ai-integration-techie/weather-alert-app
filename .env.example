# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
BIGQUERY_DATASET_ID=weather_data
BIGQUERY_LOCATION=US

# Agent Engine Configuration
AGENT_ENGINE_URL=https://your-region-aiplatform.googleapis.com
AGENT_REGION=us-central1

# National Weather Service Configuration
NWS_USER_AGENT=WeatherInsightsAdvisor/1.0 (your-email@domain.com)

# Server Configuration
PORT=8080
NODE_ENV=production

# Optional: Service Account Key (if not using default credentials)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json